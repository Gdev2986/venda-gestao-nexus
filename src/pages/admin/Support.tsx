
import SupportAdmin from "./SupportAdmin";

// We're creating this component to properly redirect to the SupportAdmin component
const Support = () => {
  return <SupportAdmin />;
};

export default Support;
