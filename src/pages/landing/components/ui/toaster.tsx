
// This component is for the landing page and should be minimal
// We'll just render nothing since the main toaster is in main.tsx
import React from "react";

export function Toaster() {
  // This component doesn't render anything since we use sonner Toaster in main.tsx
  return null;
}
