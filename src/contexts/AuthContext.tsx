
// Compatibility layer - exports the same auth functionality from the provider
export { useAuth, AuthContext } from "@/providers/AuthProvider";
export { default as AuthProvider } from "@/providers/AuthProvider";
